#[derive(Debug, Copy, Clone, PartialEq, Eq, Hash, Serialize)]
pub enum Instruction {
    {{#each instructions}}
    {{this.name}}({{this.name}}Instruction),
    {{/each}}
}

{{#each instructions}}
{{#if this.description}}
/// {{this.description}}
{{/if}}
#[derive(Debug, Copy, Clone, PartialEq, Eq, Hash, Serialize)]
pub struct {{this.name}}Instruction {
    {{#each this.fields}}
    pub {{this.name}}: {{this.type}},
    {{/each}}
}
{{/each}}