import core/collection ( map_sublists, map )
import ./cards and (Card)

function standard(): List<Card> {
	cards.Suit.all->map_sublists(fn(suit) {
		cards.Rank.all->map(fn(rank) {
			Card(rank=rank, suit=suit)
		})
	})
}
